services:
  api:
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "5002:5000"

    environment:
      NODE_ENV: production
      PORT: 5000
      MONGO_URI: ${MONGO_URI}
      JWT_SECRET: ${JWT_SECRET}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY}

    restart: unless-stopped
